# Coding

- TypeScript `strict` everywhere
- Forbidden: `any` in money, auth, legal paths
- Money: Prisma `Decimal` + string contracts in `@dodo/shared-types`
- Nest (from step 9): Controller → Service → Repository
- No tax engine in the browser
